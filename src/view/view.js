export const displayAvailableLottoCount = (lottoCount) => {
  return console.log(lottoCount);
};

export const displayLottoNumbers = (lottoNumbers) => {
  lottoNumbers.forEach((lotto) => console.log(lotto));
};
